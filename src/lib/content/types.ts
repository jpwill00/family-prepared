// Built-in content type definitions — Sprint 1 will flesh these out.

export interface ContentAreaMeta {
  content_type: string;
  title: string;
  icon?: string;
  sources?: string[];
  last_reviewed?: string;
}
