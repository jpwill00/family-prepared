# Post-MVP Backlog

Ranked deferred items. Trigger conditions specify when each item becomes worth addressing.

| Item | Priority | Trigger to address |
|------|----------|--------------------|
| GitHub OAuth Device Flow + read/commit sync | P1 | After MVP feedback confirms users want roundtrip; OAuth app registered |
| Community pack registry + browse/install UI | P1 | After ≥3 community-published packs exist outside the seed library |
| Pack updates with diff/accept (respect local edits) | P2 | Once any pack has a v2 |
| Pack publishing direct-to-GitHub | P2 | When users ask "how do I share?" without ZIP friction |
| Encryption of `secure: true` fields (Web Crypto AES-GCM) | P1 | When a pilot family wants to store SSNs/insurance numbers |
| GeoJSON drawing UI (leaflet-draw) | P3 | When pasted GeoJSON proves too technical for users |
| Challenge-response codes module | P2 | After encryption ships |
| Multi-device sync via GitHub commits | P1 | Same trigger as GitHub sync |
| Dark mode | P3 | User demand; use CSS variables already in place |
| AI plan generation + library-aware Q&A | P2 | When users hit "I don't know what to write" — instrument first |
| PACE auto-routing (network detection) | P3 | After users have any cell of contacts to route to |
| Pack signing / verification | P2 | When trust matters (e.g., a malicious pack is reported) |
| LoRa/mesh integration | P4 | Long horizon — needs hardware partner |
| Native packaging (Capacitor) | P3 | If iOS App Store presence is wanted |
| Offline map tile pre-loading for user's region | P2 | Once map usage data shows offline map need |
| Import from FEMA Ready.gov export | P3 | If FEMA publishes an export format |
